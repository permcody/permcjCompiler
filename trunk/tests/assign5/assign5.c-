// C-F06
void main(void)
{
    int x; int y; int z;
    x = 333;
    y = 444;
    z = 666;

    x = y = z;
    output(x); output(y); output(z); outnl();
    x = x + z;
    output(x); output(y); output(z); outnl();
    x = y = y + z;
    output(x); output(y); output(z); outnl();

    x = y = z;
    output(x); output(y); output(z); outnl();
    x = x - y
    output(x); output(y); output(z); outnl();
    x = y = y - z;
    output(x); output(y); output(z); outnl();

    return;
}
