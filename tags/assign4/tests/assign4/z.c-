//
// Randomly generated C- code
//
bool w(void)
{
    void w;
    bool v;
    while (-v)
	if (true + v) {
	    bool v;
	    while (-11 - 3) {
	    }
	}
	else;
    if (u = !y) {
	bool z;
	y()>--x;
	true;
    }
    else if (w = 0) { {
    }
    }
    else if (u())
	if (x + 7 - v)
	    v;
    x = u%true/w;
}
